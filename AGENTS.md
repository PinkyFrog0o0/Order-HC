# Haycargo 清关系统 — AGENTS.md

> 项目开发准则。改编自 [andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)
> 的四条核心原则，并结合本项目（多租户清关业务系统）的实际场景做了项目化定制。

---

## 关于本项目

**Haycargo** 是一个面向货主 + 清关/物流服务商的多租户业务系统，覆盖：

- **客户端**：询价订单创建（Excel 模板解析 + 装箱单/商业发票上传）、订单管理
- **管理端**：清关（询价/报价/清关行配置/成本配置/报价配置）、卡车（LTL/FTL/港到门）、系统管理、基础设置、报表

**业务红线**（任何决策都要先回到这里）：
1. **多租户隔离** — 客户数据严格隔离，任何查询/写入都要带 `tenant_id`
2. **审计可追溯** — 状态变更、金额改动、删数据必须有日志
3. **文件溯源** — 上传的装箱单/发票必须保留原件 + 解析结果
4. **配置驱动** — 成本/报价/角色权限都走配置表，不写死在代码里

---

## 四条核心准则

### 1. Think Before Coding — 编码前先想清楚

LLM 容易替用户做假设然后闷头干，这条原则强制显式推理：

- **明确说出假设** — 不确定就问，不要猜
- **列出多种解读** — 模糊时不要默默选一种
- **该顶回去就顶回去** — 如果有更简单的方案，直接说
- **卡住就停下** — 说出哪里不清楚，问清楚再继续

**本项目实践**：
- 拿到"创建询价单"这种模糊需求时，先列：① 字段从哪来（Excel？手工？两者？）② 草稿和提交的区别是什么 ③ 上传文件是必填还是可选 ④ 谁能看到草稿
- 不要假设"Excel 模板长什么样"——必须先看到模板或客户描述

---

### 2. Simplicity First — 简单优先

**用最少的代码解决问题，不做任何投机性的设计。**

反对过度工程的倾向：
- 不写超出需求的功能
- 不为只用一次的代码抽抽象层
- 不加未要求的"灵活性"/"可配置性"
- 不为不可能发生的场景写错误处理
- 200 行能解决的事不要写成 1000 行

**测试标准**：一个有经验的高级工程师看了会不会说"这太复杂了"？如果是，简化。

**本项目实践**：
- 不要先搭一套"通用规则引擎"，先用一个 `if-else` 撑起 MVP
- 不要预设"将来要支持 100 个租户"，先把当前 5 个客户跑通
- 不要为了"完美抽象"把询价单/报价单/业务单强行共用一套模型
- Excel 解析先用 `xlsx` 库直接读字段，不要先封装一层"模板适配器"

---

### 3. Surgical Changes — 手术刀式改动

**只动必须动的地方，只清理自己造成的烂摊子。**

修改已有代码时：
- 不要顺手"改进"相邻代码、注释、格式
- 不要重构没坏的东西
- 匹配已有风格，即使你有不同偏好
- 看到无关的 dead code，**说出来**而不是直接删

你自己改动造成的孤儿代码必须清理：
- 你新加的 import/变量/函数变成无用了 → 删
- 改动前就存在的 dead code → 不删，除非用户说删

**测试标准**：每一行改动都能直接对应到用户的需求。

**本项目实践**：
- 改一个表的字段时，不要顺手"优化"邻接表的索引
- 看到历史代码里某个函数没人调用，**告诉用户**，不要自作主张删
- 不要因为"风格更统一"就重写整个模块的命名

---

### 4. Goal-Driven Execution — 目标驱动的执行

**定义成功标准，循环到验证通过。**

把命令式任务转成可验证的目标：

| 不要这样 | 要这样 |
|---------|--------|
| "加上传校验" | "写测试覆盖非法文件类型/超大文件/空文件，跑通" |
| "修复报价 bug" | "写一个能复现 bug 的测试，再让它通过" |
| "重构成本配置" | "重构前后跑同一套测试，结果一致" |

多步任务要先列计划：
```
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

**强成功标准让 LLM 能独立循环，弱标准（"让它跑起来"）需要不断澄清。**

**本项目实践**：
- 每个模块交付时附带：① 至少一个 happy path 端到端测试 ② 关键失败场景测试（权限拒绝、租户隔离、文件解析失败）
- 改 SQL schema 时必须有 migration 脚本 + 回滚脚本
- 上线前 checklist：审计日志记录了吗？租户隔离测了吗？文件原件存了吗？

---

## 项目特定约定

### 技术栈（本项目基线，可讨论）
- 前端：React + TypeScript + Ant Design Pro
- 后端：NestJS（TypeScript）
- 数据库：PostgreSQL（启用 RLS）+ Redis
- 文件存储：S3 兼容（MinIO / 阿里云 OSS）
- Excel 解析：`exceljs`

### 命名约定
- 表名：snake_case 复数（`inquiry_orders`, `clearance_quotes`）
- 字段名：snake_case（`tenant_id`, `created_at`）
- TS 类/组件：PascalCase（`InquiryOrderForm`）
- TS 函数/变量：camelCase（`parseExcelTemplate`）
- 业务编号：`{类型前缀}-{年份}-{租户代码}-{6位流水}`，例：`INQ-2026-XM001-000123`

### Git 提交约定
- `feat:` 新功能
- `fix:` 修复
- `refactor:` 重构（无行为变化）
- `docs:` 文档
- `test:` 测试
- `chore:` 杂项（依赖、配置）

每个 PR 必须能回答：**这次改动对应的用户需求是什么？**

---

## Tradeoff 说明

这套准则偏向**谨慎而非速度**。对于琐碎任务（typo 修复、明显的一行改动），自行判断——不是每次改动都需要全套严格流程。

目标是**减少非琐碎工作上的昂贵错误**，而不是拖慢简单任务。

---

## 参考

- 原始仓库：https://github.com/forrestchang/andrej-karpathy-skills
- Karpathy 原推：https://x.com/karpathy/status/2015883857489522876